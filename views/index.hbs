<div id="map">
{{#each runningTrackArr}}
    {{#if @first}}
        <div id="destMap" data-name="{{name}}" data-lat="{{lat}}" data-lng="{{lng}}"></div>
    {{/if}}
{{/each}}
{{#each runningTrackArr}}
    	<div class="destMarker" data-name="{{name}}" data-lat="{{lat}}" data-lng="{{lng}}"></div>
{{/each}}

</div>

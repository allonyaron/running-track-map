<div id="map">
{{#each runningTrackArr}}
    {{#if @first}}
        <div id="destMap" data-name="{{Name}}" data-lat="{{lat}}" data-lng="{{lon}}"></div>
    {{/if}}
{{/each}}
{{#each runningTrackArr}}
    {{#unless @first}}
    	<div class="destMarker" data-name="{{Name}}" data-lat="{{lat}}" data-lng="{{lon}}"></div>
    {{/unless}}	
{{/each}}

</div>